name: E2E CI

on:
  push:
    branches:
      - main

jobs:
  e2e:
    name: end-to-end
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Start services
        run: |
          docker compose -f agent/test/integration/docker-compose.yml up -d --build

      - name: Run integration tests
        # TODO: replace with real test runner once e2e tests are implemented.
        run: echo "TODO: run e2e tests"

      - name: Tear down
        if: always()
        run: |
          docker compose -f agent/test/integration/docker-compose.yml down
